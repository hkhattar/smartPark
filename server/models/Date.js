
console.log('park.model')

var mongoose = require('mongoose');
var Schema = mongoose.Schema;
var DateSchema = new mongoose.Schema({

});

mongoose.model('Date',DateSchema); //attaches the User constant variable to mongoose


